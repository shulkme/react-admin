import { createStyles } from 'antd-style';

const useStyles = createStyles(({ css }) => {
  return {
    root: css`
      min-height: 100vh;
    `,
    header: css``,
    content: css``,
    footer: css`
      text-align: center;
    `,
    sider: css``,
  };
});

export default useStyles;
