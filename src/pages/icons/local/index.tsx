import PageContainer from '@/components/page-container';
import type React from 'react';

const Local: React.FC = () => {
  return (
    <PageContainer size="large" title="Local Icons">
      local
    </PageContainer>
  );
};

export default Local;
