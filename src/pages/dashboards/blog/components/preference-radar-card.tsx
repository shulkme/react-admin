import { Card } from 'antd';
import type React from 'react';

const PreferenceRadarCard: React.FC = () => {
  return (
    <Card bordered={false} title="Preference" style={{ height: '100%' }}></Card>
  );
};

export default PreferenceRadarCard;
